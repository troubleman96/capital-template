# Capital Web Application

Capital is an AI-native data platform web application built with React, Vite, Tailwind CSS, and TanStack Start.

## Development

To get started locally:

```sh
bun install
bun run dev
```

## Production Build

To generate the production build:

```sh
bun run build
```
